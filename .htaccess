Options -Indexes

RewriteEngine On
RewriteBase /

# Ignorer les fichiers et dossiers existants (assets, images…)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rediriger tout vers index.php
RewriteRule ^(.*)$ index.php [QSA,L]
